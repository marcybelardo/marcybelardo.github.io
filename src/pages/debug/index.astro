---
import { getCollection } from "astro:content";

const posts = await getCollection("blog");
const names = posts.map(p => ({ slug: p.slug, title: p.data.title }));
---
<html>
<body>
<h1>Debug</h1>
<p>Found {posts.length} blog posts</p>
<pre>{JSON.stringify(names, null, 2)}</pre>
</body>
</html>
